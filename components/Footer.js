import { Container } from '@chakra-ui/react';

const Footer = () => {
    return (
        <Container as="footer" p={4} textAlign="center">
            This is the footer
        </Container>
    );
};

export default Footer;
